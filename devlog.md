# 開発ログ (devlog.md) — 自由診療専用ページ

## [2026-06-27 12:20] Step 1-1: プロジェクト新規立ち上げと初期環境構築
**ステータス**: ✅ DONE

**作業内容**:
- `projects/clinic/private-services-site` ディレクトリの作成。
- プロジェクト独自の開発標準 [AGENTS.md](file:///c:/Users/coino/AntigravityWorkspace/projects/clinic/private-services-site/AGENTS.md) の作成。
- スプレッドシート連携とプレミアムデザイン要件を含む [requirements.md](file:///c:/Users/coino/AntigravityWorkspace/projects/clinic/private-services-site/requirements.md) の作成。

**動作確認結果**:
- 初期設計およびドキュメンテーションの構築完了。

---

## [2026-06-27 12:21] Step 1-2: フロントエンド実装とローカルGitコミットの完了
**ステータス**: ✅ DONE

**作業内容**:
- HTML/CSS/JS (`index.html`, `style.css`, `app.js`) による自由診療専用のプレミアムフロントエンドの実装。
- カテゴリ別タブ切り替え、および詳細情報を滑らかに展開するアコーディオンリストの実装。
- 万が一のAPIエラーに備えた `menu_fallback.json` による安全フォールバック設計の導入。
- スプレッドシート自動初期セットアップ用のGASスクリプト [setup_private_services_spreadsheet.js](file:///c:/Users/coino/AntigravityWorkspace/projects/clinic/private-services-site/scratch/setup_private_services_spreadsheet.js) を作成。
- `git init` およびローカルでのファーストコミットを完了。

**動作確認結果**:
- ビルドスクリプト `build.py` の実行成功。
- ローカル開発サーバー `python -m http.server 8081` を経由して、ブラウザ上で正常にアセットがロードされ、UIが崩れずに動作することを確認。

---

## [2026-06-29 12:04] Step 1-3: 自由診療と一般自費・健診項目の掲載分離
**ステータス**: ✅ DONE

**作業内容**:
- スプレッドシートの C列 に `group` カラムを追加し、「自由診療」「一般健診・書類」「非掲載」を管理できる設計に変更。
- `parse_master.py` を改修し、マスタデータの98件を新しい `group` ルールで自動判別・再構成。
- 不要またはWeb掲載に不適切な「処方箋FAX代」「オンラインシステム料」「領収書再発行」などの雑費を自動で `status: "非表示"` に設定。
- フロントエンド (`index.html`, `style.css`, `app.js`) を改修し、`group === '自由診療'` は上部のプレミアムカード＆タブで表示し、`group === '一般健診・書類'` は下部にアコーディオン付き料金テーブルで整理して表示する振分けロジックを実装。
- 更新されたファイルをビルドし、GitHubにプッシュ（Actionsによる自動デプロイが開始）。

**動作確認結果**:
- ビルドとGitプッシュが正常に成功。

---

## [2026-06-29 12:15] Step 1-4: 医療広告ガイドライン対応（効果・リスク等の明示とUI改修）
**ステータス**: ✅ DONE

**作業内容**:
- `parse_master.py` 内に主要な自費注射・点滴項目（プラセンタ、高濃度ビタミンC、美白注射、グルタチオン等）および経口薬（リベルサス、マンジャロ）に関する一般的な「効能・効果（説明）」「所要時間」「副作用・注意点」のマッピング定義を追加し、fallback JSON とセットアップスクリプトを更新。
- フロントエンド (`app.js`, `style.css`) を改修し、自由診療の各カード価格下に「※自由診療にはリスク・副作用があります」というガイドラインに準拠した注意喚起を常時表示（同一視野での明示要件）。
- アコーディオン開閉時に、副作用・注意事項部分を赤文字の警告アイコン付きの目立つスタイル（`.detail-section-title.warning`）で表示する自動整形整形ロジックを実装。
- 更新されたファイルをビルドし、GitHubにプッシュ（FTPタイムアウトエラー後の再試行によりデプロイ成功）。

**動作確認結果**:
- ビルドとGitプッシュが正常に成功。GitHub ActionsのFTP Deploy完了を確認。

---

## [2026-07-16 13:16] Step 1-5: タミフル予防薬および予防接種の検診メニュー移行
**ステータス**: ✅ DONE

**作業内容**:
- `parse_master.py` のグループ振り分けロジックを改修。タイトルに「タミフル」を含む項目、およびカテゴリが「予防接種」である全項目の配信先グループを、`自由診療` (Wellness) から `一般健診・書類` (検診メニュー) へ変更するよう設定。ただし、予防接種のうち「定期」「免除」にあたる項目は引き続き `status: "非表示"` とした。
- `parse_master.py` を実行し、`src/data/menu_fallback.json`（フォールバック用JSON）および `scratch/setup_private_services_spreadsheet.js`（GASスプレッドシート初期化スクリプト）の初期データを最新の振り分けルールに基づいて再生成。
- ビルドスクリプト `tools/build.py` を実行し、`dist/` ディレクトリに反映。

**特記事項・判断メモ**:
- 本サイトはスプレッドシート（GAS）から動的にデータを読み込む設計となっている。スプレッドシート上の実データを破壊せず安全に移行するため、スプレッドシートに関しては「C列（group）」を `自由診療` から `一般健診・書類` へ手動書き換えしてもらうようユーザーへ案内する方針をとった。
- タミフル予防薬（内服薬・サプリメント）は、検診メニュー側に移動することで自動的に「内服薬・サプリメント」という新しいアコーディオンとして下部に組み込まれる。

**動作確認結果**:
- 一時的に `app.js` の API 読み込みをオフにし、ローカルフォールバックデータ `menu_fallback.json` を使用する形でローカル開発サーバー（Python HTTP Server）にて画面を検証。
- 期待通り、上部「Wellness」セクションからタミフルおよび予防接種が消え、下部「健康診断・各種書類」アコーディオン一覧に「予防接種」および「内服薬・サプリメント」が並び、アコーディオンを開くとそれぞれ対象項目がテーブル形式で正しく表示されることを確認した。

---

## [2026-07-16 13:40] Step 1-6: スプレッドシート分割およびグループ名変更対応（GAS・フロント改修）
**ステータス**: ✅ DONE

**作業内容**:
- **フロントエンド（`app.js`）の改修**:
  - `group === "一般健診・書類"` 以外のすべてのデータを `wellnessItems` として分類するように変更。
  - 上部Wellnessのタブ切り替えを、従来の `category` 基準から新しい `group`（ダイエット、増毛、美白、健康増進）基準で動的生成・フィルタリングされるように変更。これにより、スプレッドシート側でセルごとに色分け管理されたグループが、そのままサイト上のタブメニューとして反映されるシームレスな仕様へとアップグレードした。
- **マスタデータ判定スクリプト（`parse_master.py`）の改修**:
  - `determine_group_and_status` に、自費診療の各項目を新しいグループ分類（ダイエット、増毛、美白、健康増進）へとルーティングするマッピング定義を追加。
  - スクリプトを実行し、`src/data/menu_fallback.json` およびセットアップスクリプトを更新。
- **GAS (Google Apps Script) 用コードの作成**:
  - スプレッドシート内の「自由診療」と「健診メニュー」の2つのシートからデータをループ処理で抽出し、マージした1つのJSON配列として配信する新しい `doGet` プログラムを作成。

**動作確認結果**:
- `GAS_API_URL` を本番URLに戻して `build.py` でビルド。Gitコミットとリモート（mainブランチ）プッシュを完了。

---

## [2026-07-16 14:56] Step 1-7: Googleマップ埋め込み座標の修正
**ステータス**: ✅ DONE

**作業内容**:
- `index.html` にあるアクセス案内用の Google Map（iframe）の埋め込みURL（`src`）を、ユーザーより提示された正確な緯度経度座標（`35.971393685875405, 139.77201859316477`）を用いたピン留め表示形式に変更。
  - 変更URL: `https://maps.google.com/maps?q=35.971393685875405,139.77201859316477&z=17&output=embed&hl=ja`
- `build.py` を実行して `dist/` に成果物を反映。
- 変更ファイルを Git コミットおよび GitHub (`main`ブランチ) へのプッシュを完了し、自動デプロイをトリガーした。

**動作確認結果**:
- ビルドおよびプッシュの正常終了を確認。

---

## [2026-07-16 15:03] Step 1-8: 健康診断セクションの完全削除およびGoogleマップズーム調整
**ステータス**: ✅ DONE

**作業内容**:
- **健康診断・各種書類セクションの完全削除**:
  - 本サイト（Wellness）での健康診断取り扱い終了に伴い、`index.html` から `<section id="general-section">` およびヘッダーのナビゲーションリンクを完全に削除。
  - `app.js` からも不要な健診データフェッチ処理（`?sheet=健診メニュー`）、描画関数 `renderGeneralMenu`、およびアコーディオン用イベントハンドラ `setupGeneralAccordion` を削除してコードをクリーンアップ。
- **Googleマップズーム調整**:
  - アクセス案内マップ（iframe）の初期表示範囲が詳細すぎるのを防ぎ、主要な路線や駅等の位置関係を把握しやすくするため、URLのズームパラメータを `z=17` から **`z=15`** へ変更（ズームアウト）。
- `parse_master.py` の自動判別ロジックを修正し、一般健診・血液検査等の項目はすべて `非掲載/非表示` になるようマッピングを調整して `menu_fallback.json` を更新。
- `build.py` を実行して `dist/` へのビルド成果物反映後、GitHubへプッシュ（自動デプロイが開始）。

---

## [2026-07-16 16:06] Step 1-9: ブラウザキャッシュ対策（スクリプトバージョン更新）
**ステータス**: ✅ DONE

**作業内容**:
- ブラウザが古い `app.js` をキャッシュから読み込み続けてメニュー非表示バグを引き起こす問題を解決するため、`index.html` にて読み込む `app.js` のクエリパラメータを `v=2026071601` へ更新。
- `build.py` を再実行して `dist/index.html` へ反映。
- 変更ファイルを Git コミット・プッシュし、本番自動デプロイをトリガーした。

---

## [2026-07-16 16:22] Step 1-10: リスク警告文の配置移動およびメニュー説明文の修正
**ステータス**: ✅ DONE

**作業内容**:
- **メニュー説明文の修正**:
  - `index.html` に記述されている「スプレッドシートから最新のメニューと価格情報をリアルタイムに取得しています。」という内部システム向けの開発用文言を、患者向けの「当クリニックで提供している美容注射・点滴、医療ダイエットなどの自費診療メニューです。」という正式な案内テキストへ修正。
- **リスク警告文の移動**:
  - 各カードの表面（閉じた状態）に露出していた「※自由診療にはリスク・副作用があります。詳細は下記をご確認ください。」を削除。
  - アコーディオン詳細パネル（`.menu-detail-content`）を展開した際の一番下に、破線ボーダー（`border-top: 1px dashed var(--color-border)`）を挟んで、小さく（`font-size: 0.75rem`）控えめな色で警告を表示するように `app.js` を改修。
- `build.py` の実行、バージョン指定を `v=2026071602` に更新してキャッシュバスティング。
- Git プッシュを完了。






